# OpenClosed - Voice Identification App

Aplikasi Streamlit untuk mengidentifikasi suara buka dan tutup menggunakan model machine learning.

## Cara Menjalankan
1. Install dependensi: `pip install -r requirements.txt`
2. Jalankan aplikasi: `streamlit run app.py`
